/**
 * Created by vuchien on 7/17/16.
 */
'use strict';
import { guid } from '../components/commons';

const initialState = {
  itemsDrag: [
    { id: guid(), value: 'Item 1', top: '0px', left: '0px' },
    { id: guid(), value: 'Item 2', top: '10px', left: '20px' },
  ],
};

export default function testDragReducers(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
